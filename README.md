# search_agent

这是一个面向搜索增强问答的项目。

它主要做三件事：

- 让模型先搜索，再回答短问答问题。
- 对问答结果做自动评测，统计准确率。
- 保留训练、评测和模型转换相关脚本，方便继续迭代。

仓库里比较重要的目录：

- `search_agent/`：项目主代码，包含统一的命令行入口和核心流程。
- `eval/`：评测脚本。
- `data/`：问答数据集。
- `verl/`、`LlamaFactory/`：训练相关的上游框架和脚本。
